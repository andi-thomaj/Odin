name: Backend tests

on:
  pull_request:
  push:
    branches: [master, development]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  unit-tests:
    name: Odin.Api.Tests (unit)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', 'Directory.Packages.props') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore
        run: dotnet restore Odin.slnx

      - name: Build
        run: dotnet build Odin.slnx --configuration Release --no-restore

      - name: Run unit tests
        run: |
          dotnet test Odin.Api.Tests/Odin.Api.Tests.csproj \
            --configuration Release \
            --no-build \
            --logger "trx;LogFileName=unit-tests.trx" \
            --logger "console;verbosity=normal" \
            --results-directory ./TestResults

      - name: Upload TRX results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-results
          path: TestResults/*.trx
          if-no-files-found: warn
          retention-days: 14

  integration-tests:
    name: Odin.Api.IntegrationTests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', 'Directory.Packages.props') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Show Docker version (Testcontainers prereq)
        run: docker version

      - name: Restore
        run: dotnet restore Odin.slnx

      - name: Build
        run: dotnet build Odin.slnx --configuration Release --no-restore

      - name: Run integration tests
        run: |
          dotnet test Odin.Api.IntegrationTests/Odin.Api.IntegrationTests.csproj \
            --configuration Release \
            --no-build \
            --logger "trx;LogFileName=integration-tests.trx" \
            --logger "console;verbosity=normal" \
            --results-directory ./TestResults
        env:
          # Force the Testcontainers path; ignore any developer connection string that might
          # leak in via shell env or user secrets on a self-hosted runner.
          ConnectionStrings__DefaultConnection: ""

      - name: Upload TRX results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-test-results
          path: TestResults/*.trx
          if-no-files-found: warn
          retention-days: 14
